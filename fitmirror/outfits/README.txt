Drop any outfit images here (.png, .jpg, .webp).

After adding or removing files, refresh the outfit list (pick one):

  Option A — from the fitmirror folder:
    py generate_manifest.py

  Option B — start the backend (double-click backend/start-backend.bat), then refresh the app.

File names become labels: "rando.png" → "Rando".
You do not need to edit main.js.
